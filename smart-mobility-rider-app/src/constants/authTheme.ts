// Auth screens import from here; values now live in theme.ts (shared across the whole app).
// Kept as a re-export so Login/Register don't need touching when the shared palette moved.
import { colors, spacing } from "./theme";

export const authColors = colors;
export const authSpacing = spacing;
